annotated-types==0.8.0
anyio==4.14.2
asgiref==3.12.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.0
cfgv==3.5.0
click==8.4.2
colorama==0.4.6
cryptography==49.0.0
distlib==0.4.3
distro==1.9.0
Django==6.0.7
django-cors-headers==4.9.0
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
docstring_parser==0.18.0
Faker==40.35.0
filelock==3.32.0
h11==0.16.0
httpcore==1.0.9
httpcore2==2.9.1
httpx==0.28.1
httpx2==2.9.1
identify==2.6.19
idna==3.18
jiter==0.16.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
mcp==2.0.0
mcp-types==2.0.0
nodeenv==1.10.0
opentelemetry-api==1.44.0
pillow==12.3.0
platformdirs==4.11.0
pre_commit==4.6.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
PyJWT==2.13.0
python-decouple==3.8
python-discovery==1.5.0
python-multipart==0.0.32
# Windows-only, and nothing in this project imports it — it arrived as a
# transitive pin from a local install. Unmarked it hard-fails `pip install` inside
# the Linux container, which is a confusing way to lose ten minutes.
pywin32==312; sys_platform == "win32"
PyYAML==6.0.3
referencing==0.37.0
rpds-py==2026.6.3
sniffio==1.3.1
sqlparse==0.5.5
sse-starlette==3.4.6
starlette==1.3.1
truststore==0.10.4
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.3
uvicorn==0.52.0
virtualenv==21.7.0
coverage==7.15.3
drf-spectacular==0.30.0
inflection==0.5.1
uritemplate==4.2.0

# Serving in a container.
gunicorn==26.0.0
# Lets Django serve its own collected static files with correct cache headers, so
# the admin, the browsable API and /api/docs/ stay styled under DEBUG=False
# without putting nginx in front of the backend.
whitenoise[brotli]==6.12.0
# Swagger UI assets as a package instead of a CDN fetch — /api/docs/ then works
# with no internet.
drf-spectacular-sidecar==2026.8.1
# Client for Django's built-in RedisCache backend (not django-redis).
redis==8.1.0

# LLM provider SDKs.
#
# These were installed in the local virtualenv but never added here, which went
# unnoticed because both imports are lazy — they sit inside the provider's
# __init__, so the app boots and every test passes as long as both roles are on
# `stub`. The container is what exposed it: a clean `pip install -r` produced an
# image where switching EXTRACTION_PROVIDER to anthropic raises
# ModuleNotFoundError at the first extraction, mid-demo.
#
# openai is not a typo — DeepSeek exposes an OpenAI-compatible API and
# deepseek_provider.py talks to it with `from openai import OpenAI`.
anthropic==0.120.2
openai==2.51.0
